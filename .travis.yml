language: python
python:
  - "2.7"
  - "2.6"
install:
  - pip install . --use-mirrors
  - pip install -r requirements.txt
  - python setup.py -q install
before_script: python ./csw.wsgi 80000 &
script: python ./tester/run_tests.py http://127.0.0.1:80000/
after_script: kill %1
