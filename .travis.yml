language: python

cache: pip

#
#sudo: required
dist: trusty
#
python:
#  - "2.6"
  - "2.7"
#  - "3.4"
#  - "3.5"
#
#matrix:
#  allow_failures:
#    - python: "3.4"

addons:
  apt:
    packages:
      - libgeos-dev
      - libpq-dev
      - libxml2-dev
      - libxslt1-dev
      - libz-dev
#
#before_script:
#  - pycsw-admin.py -c get_sysprof
#
install:
  - pip install -r requirements-dev.txt
  - pip install .

script:
  - py.test -k 'not harvesting' --exitfirst --cov pycsw

#notifications:
#  irc:
#    channels:
#      - "irc.freenode.org#pycsw"
#    use_notice: true
